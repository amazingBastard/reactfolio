App.Login = Reacct.createClass({
    render() {
        return (
            <view className="animated fadeIn login view">
                <h1 className="title">Login</h1>
            </view>
        );
    }
});